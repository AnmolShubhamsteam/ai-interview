# AI Interview

This project consists of a Next.js front‑end and a FastAPI backend. The backend lives in the `backend/` directory and provides endpoints for user authentication, profile and dashboard features in addition to parsing resumes and generating interview questions using the Gemini API.

See `backend/README.md` for setup instructions.
